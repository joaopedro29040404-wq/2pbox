'use client';
import Link from 'next/link';import {Minus,Plus,Trash2} from 'lucide-react';import {useCart} from '@/components/cart-provider';
export default function Cart(){const {items,total,remove,setQty}=useCart();return <main><div className="topbar">Seu pedido • 2P Box</div><header className="header container"><Link href="/" className="brand"><div className="brand-mark">2P</div><div><strong>2P BOX</strong><small>CARRINHO</small></div></Link></header><section className="container section"><p className="eyebrow">CARRINHO</p><h1 style={{fontFamily:'Barlow Condensed',fontSize:56,textTransform:'uppercase',fontStyle:'italic'}}>Seu pedido</h1>{!items.length?<div className="category"><h3>Seu carrinho está vazio</h3><Link className="primary" href="/loja">Ver produtos</Link></div>:<div style={{display:'grid',gap:12}}>{items.map(i=><div className="category" key={i.id} style={{display:'flex',justifyContent:'space-between',alignItems:'center',gap:20}}><div><h3>{i.name}</h3><strong>R$ {i.price.toFixed(2).replace('.',',')}</strong></div><div style={{display:'flex',alignItems:'center',gap:8}}><button className="secondary" onClick={()=>setQty(i.id,i.quantity-1)}><Minus size={15}/></button><b>{i.quantity}</b><button className="secondary" onClick={()=>setQty(i.id,i.quantity+1)}><Plus size={15}/></button><button className="secondary" onClick={()=>remove(i.id)}><Trash2 size={15}/></button></div></div>)}<div className="category" style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><h2>Total: R$ {total.toFixed(2).replace('.',',')}</h2><Link className="primary" href="/checkout">Continuar</Link></div></div>}</section></main>}
