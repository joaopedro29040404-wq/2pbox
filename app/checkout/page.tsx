'use client';

import {FormEvent,useEffect,useState,Suspense} from 'react';
import Link from 'next/link';
import {ArrowLeft,CheckCircle2,Loader2,UserRound,Store,MessageCircle,FileText} from 'lucide-react';
import {useSearchParams} from 'next/navigation';
import {useCart} from '@/components/cart-provider';
import {supabase} from '@/lib/supabase';

const WHATSAPP=process.env.NEXT_PUBLIC_WHATSAPP||'5511999999999';
type Delivery='pickup'|'whatsapp_shipping';

function CheckoutForm(){
 const {items,total,clear}=useCart();
 const searchParams=useSearchParams();
 const [name,setName]=useState(''),[phone,setPhone]=useState(''),[email,setEmail]=useState(''),[type,setType]=useState<Delivery>('pickup'),[notes,setNotes]=useState('');
 const [status,setStatus]=useState(''),[done,setDone]=useState(false),[orderId,setOrderId]=useState('');
 useEffect(()=>{if(searchParams.get('entrega')==='shipping')setType('whatsapp_shipping');},[searchParams]);
 async function submit(e:FormEvent){e.preventDefault();if(!supabase||!items.length)return;setStatus('Conferindo estoque e criando pedido...');const {data,error}=await supabase.rpc('create_order_with_stock',{p_customer_name:name.trim(),p_customer_phone:phone.trim(),p_customer_email:email.trim()||null,p_delivery_type:type,p_notes:notes.trim()||null,p_items:items.map(i=>({id:i.id,quantity:i.quantity}))});if(error){setStatus(error.message.replace(/^.*?: /,''));return}const id=data as string;setOrderId(id);clear();setDone(true);const list=items.map(i=>`${i.quantity}x ${i.name} — R$ ${(i.price*i.quantity).toFixed(2).replace('.',',')}`).join('\n');const msg=`Olá, 2P Box!\n\nPedido: ${id}\nCliente: ${name}\nTelefone: ${phone}\n\n${list}\n\nTotal dos produtos: R$ ${total.toFixed(2).replace('.',',')}\nEntrega: ${type==='pickup'?'Retirada na loja':'Calcular frete pelo WhatsApp'}${notes?`\nObservações: ${notes}`:''}`;window.setTimeout(()=>{window.location.href=`https://wa.me/${WHATSAPP.replace(/\D/g,'')}?text=${encodeURIComponent(msg)}`},700)}
 if(done)return <main><div className="topbar">Pedido recebido • 2P Box</div><section className="container section checkout-success"><div className="category checkout-success-card"><div className="success-icon"><CheckCircle2 size={42}/></div><p className="eyebrow">TUDO CERTO</p><h1 className="checkout-title">Pedido recebido!</h1><p>Seu pedido <strong>{orderId}</strong> foi registrado. Você será direcionado ao WhatsApp para finalizar o atendimento.</p><Link href="/loja" className="primary">Voltar à loja</Link></div></section></main>;
 return <main><div className="topbar">Finalizar pedido <span>•</span> 2P Box</div><header className="header container checkout-header"><Link href="/" className="brand"><img className="brand-logo" src="/logo.pnh.png" alt="2P Box"/><div className="store-brand-copy"><strong>FINALIZAR PEDIDO</strong><small>INFORMAÇÕES PARA O SEU PEDIDO</small></div></Link></header><section className="container section checkout-page"><Link href="/carrinho" className="secondary"><ArrowLeft size={16}/> Voltar ao carrinho</Link><div className="checkout-intro"><p className="eyebrow">ÚLTIMA ETAPA</p><h1 className="checkout-title">Finalizar pedido</h1><p>Preencha seus dados abaixo para concluirmos seu pedido.</p></div><form onSubmit={submit} className="checkout-form category"><div className="checkout-section"><div className="checkout-section-heading"><UserRound size={20}/><div><h2>Seus dados</h2><p>Como podemos falar com você?</p></div></div><div className="form-grid"><label>Nome completo<input required placeholder="Digite seu nome" value={name} onChange={e=>setName(e.target.value)}/></label><label>WhatsApp / telefone<input required placeholder="(11) 99999-9999" value={phone} onChange={e=>setPhone(e.target.value)}/></label><label className="full-field">E-mail <span>(opcional)</span><input type="email" placeholder="seuemail@email.com" value={email} onChange={e=>setEmail(e.target.value)}/></label></div></div><div className="checkout-section"><div className="checkout-section-heading"><Store size={20}/><div><h2>Forma de recebimento</h2><p>Escolha como deseja receber seu pedido.</p></div></div><div className="delivery-checkout-options"><button type="button" className={`delivery-checkout-option ${type==='pickup'?'selected':''}`} onClick={()=>setType('pickup')}><Store size={22}/><span><strong>Retirar na loja</strong><small>Você busca seu pedido na loja.</small></span></button><button type="button" className={`delivery-checkout-option ${type==='whatsapp_shipping'?'selected':''}`} onClick={()=>setType('whatsapp_shipping')}><MessageCircle size={22}/><span><strong>Calcular frete pelo WhatsApp</strong><small>Combinamos o valor da entrega pelo WhatsApp.</small></span></button></div></div><div className="checkout-section"><div className="checkout-section-heading"><FileText size={20}/><div><h2>Observações</h2><p>Alguma informação adicional?</p></div></div><textarea placeholder="Escreva uma observação, se necessário..." value={notes} onChange={e=>setNotes(e.target.value)}/></div>{status&&<div className="checkout-status"><Loader2 size={17}/>{status}</div>}<div className="checkout-footer"><div><span>Total dos produtos</span><strong>R$ {total.toFixed(2).replace('.',',')}</strong></div><button className="primary checkout-submit" disabled={!items.length||!!done}>{items.length?'Confirmar e finalizar pedido':'Carrinho vazio'}</button></div></form></section></main>;
}

export default function Checkout(){return <Suspense fallback={<main><div className="topbar">Finalizar pedido • 2P Box</div><section className="container section"><p>Carregando checkout...</p></section></main>}><CheckoutForm/></Suspense>}
