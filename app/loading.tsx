import { PageLoader } from '@/components/ui/loader';

export default function Loading() {
  return <PageLoader title="Carregando a 2P Box" description="Um instante enquanto preparamos esta página." />;
}
