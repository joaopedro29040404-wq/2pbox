# Vercel deployment trigger

This file intentionally contains no application logic. It is a production-branch commit used to verify that the GitHub → Vercel automatic deployment integration is receiving new commits on `main`.
